# API 架构说明

## 🎯 架构概述

本项目采用**统一的 API 适配层**，自动检测运行环境（Web/Electron）并选择最佳调用方式。

### 核心设计理念

- **统一接口**：业务代码只需调用统一接口，无需关心运行环境
- **自动适配**：适配层自动检测环境并选择最佳实现
- **向后兼容**：保留原有实现，平滑迁移

## 📁 文件结构

```
src/api/
├── core.ts          # 核心实现（前端 API，直接 HTTP 请求）
├── adapter.ts       # 适配层（自动选择调用方式）
├── config.ts        # 配置（环境检测）
├── index.ts         # 统一导出入口（业务代码从这里导入）
├── request.ts       # 前端请求封装（axios 配置）
└── README.md        # 本文档

src/utils/
└── web-utils.ts     # Web 环境工具函数（下载、颜色提取、文件对话框等）

electron/services/
├── api.ts           # Electron 主进程 API（仅保留差异化处理需要的功能）
├── api-request.ts   # Electron 请求封装（仅用于下载功能）
├── ipc.ts           # IPC 处理器（仅处理差异化功能）
├── palette.ts       # 颜色提取（可选，前端已实现）
└── storage.ts       # 本地存储（文件系统）
```

**注意**：`electron/services/api.ts` 现在只保留差异化处理需要的功能（如下载时获取 URL），普通 API（搜索、获取 URL、歌词、封面等）都使用前端 API。

## 🔄 调用策略

### 1. 普通 API（搜索、获取 URL、歌词、封面等）

**策略**：在 Web 和 Electron 中都使用前端 API（直接 HTTP 请求）

**原因**：
- ✅ 性能更好（无需跨进程通信）
- ✅ 代码更简单（减少 IPC 调用）
- ✅ 在 Web 和 Electron 中都能正常工作
- ✅ 不需要系统资源交互
- ✅ 减少 Electron 主进程代码量

**实现**：
```typescript
// adapter.ts
export const searchMusic = (keyword, source, page, count) => {
  // 直接调用前端 API（core.ts）
  return searchMusicImpl(keyword, source, page, count)
}

export const getLyric = (songId, source) => {
  // 直接调用前端 API（core.ts）
  return getLyricImpl(songId, source)
}

export const getPicUrl = async (picId, source, size) => {
  // 直接调用前端 API（core.ts）
  return await getPicUrlImpl(picId, source, size)
}
```

**Electron 主进程清理**：
- ❌ 已删除 `electron/services/api.ts` 中的 `getLyric`、`getPicUrl`、`getRadarPlaylist`
- ❌ 已删除 `electron/services/ipc.ts` 中的 `fetch-music-url`、`fetch-lyrics`、`get-pic-url` IPC 处理器
- ❌ 已删除 `electron/preload.ts` 中的 `fetchMusicUrl`、`fetchLyrics`、`getPicUrl` API 暴露
- ✅ 仅保留 `getSongUrl`（仅用于下载功能）

### 2. 差异化实现的功能（下载、颜色提取、文件对话框、导入导出）

**策略**：Web 和 Electron 都实现，差异化处理

**原因**：
- ✅ Web 环境可以通过浏览器 API 实现
- ✅ Electron 环境可以使用原生功能（更好的用户体验）
- ✅ 同一套代码可以在两个环境运行

**实现**：
```typescript
// adapter.ts
export const downloadMusic = async (params) => {
  // 先获取歌曲 URL
  const urlResponse = await getSongUrlImpl(...)
  const musicUrl = urlResponse?.data?.url
  
  if (isElectron() && window.electronAPI?.downloadMusic) {
    // Electron：使用 IPC（支持文件对话框选择保存位置）
    return await window.electronAPI.downloadMusic(params)
  } else {
    // Web：使用 <a> 标签下载
    return await downloadFile(musicUrl, filename)
  }
}

export const extractPalette = async (imageUrl) => {
  if (isElectron() && window.electronAPI?.extractPalette) {
    // Electron：优先使用 IPC（可能性能更好）
    try {
      return await window.electronAPI.extractPalette(imageUrl)
    } catch (error) {
      // 失败时降级到前端实现
    }
  }
  // Web 或 Electron 失败时：使用 Canvas API
  return await extractPaletteFromImage(imageUrl)
}
```

### 3. 本地存储

**策略**：Web 使用 localStorage，Electron 使用文件系统

**原因**：
- ✅ Web 环境只能使用 localStorage
- ✅ Electron 环境可以使用文件系统（更可靠）

**实现**：
```typescript
// adapter.ts
export const saveData = async (key, data) => {
  if (isElectron() && window.electronAPI?.saveData) {
    // Electron：使用文件系统
    return await window.electronAPI.saveData(key, data)
  } else {
    // Web：使用 localStorage
    localStorage.setItem(key, JSON.stringify(data))
  }
}
```

## 🚀 使用方式

### 业务代码导入

```typescript
// ✅ 推荐：从统一入口导入
import { searchMusic, getSongUrl, getLyric, downloadMusic } from '@/api'

// 所有 API 调用都会自动适配环境
const response = await searchMusic('周杰伦', 'netease', 1, 20)
const urlResponse = await getSongUrl('123456', 'netease', '320')
```

### 环境检测

```typescript
import { isElectron, isWeb } from '@/api'

if (isElectron()) {
  console.log('运行在 Electron 环境')
} else if (isWeb()) {
  console.log('运行在 Web 环境')
}
```

### Electron 专用功能

```typescript
import { downloadMusic, saveData, loadData } from '@/api'

// 在 Electron 中正常工作
// 在 Web 中会抛出错误（需要处理）
try {
  await downloadMusic({ id: '123', source: 'netease', quality: '320', filename: 'song' })
} catch (error) {
  if (error.message.includes('仅在 Electron')) {
    // Web 环境，提示用户
    alert('下载功能仅在桌面应用中可用')
  }
}
```

## 📊 调用流程图

```
┌─────────────────────────────────────────────────────────────┐
│                    业务代码（Store/Component）                  │
│                                                               │
│  import { searchMusic, getLyric, downloadMusic } from '@/api'│
└──────────────────────┬──────────────────────────────────────┘
                       │
                       ▼
┌─────────────────────────────────────────────────────────────┐
│                    API 适配层 (adapter.ts)                    │
│                                                               │
│  ┌─────────────────────────────────────────────────────┐    │
│  │  1. 检测环境：isElectron() / isWeb()                │    │
│  │  2. 选择调用方式：                                   │    │
│  │     - 普通 API → core.ts (前端 API)                 │    │
│  │     - 差异化功能 → IPC 或 Web 实现                   │    │
│  └─────────────────────────────────────────────────────┘    │
└──────────────────────┬──────────────────────────────────────┘
                       │
        ┌──────────────┴──────────────┐
        │                              │
        ▼                              ▼
┌───────────────┐          ┌──────────────────────┐
│  core.ts      │          │  window.electronAPI  │
│  (前端 API)   │          │  (IPC 调用)          │
│               │          │                      │
│  HTTP 请求    │          │  渲染进程 → IPC      │
│  (axios)      │          │       ↓              │
│               │          │  主进程 (ipc.ts)     │
│               │          │       ↓              │
│               │          │  electron/services/  │
│               │          │  (仅差异化功能)       │
│               │          │       ↓              │
│               │          │  - download-music    │
│               │          │  - extract-palette   │
│               │          │  - save-data         │
│               │          │  - import-json       │
│               │          │  - show-open-dialog  │
└───────────────┘          └──────────────────────┘
        │                              │
        └──────────────┬───────────────┘
                       ▼
              ┌─────────────────┐
              │   外部 API       │
              │  (music-api)     │
              └─────────────────┘
```

**说明**：
- 普通 API（搜索、获取 URL、歌词、封面等）统一使用前端 API（`core.ts`）
- Electron 主进程只处理差异化功能（下载、文件对话框、本地存储等）
- 不再有 IPC 调用普通 API 的路径

## 🎨 优势

### 1. 代码简洁

业务代码无需关心环境，统一调用接口：

```typescript
// ✅ 统一接口，自动适配
import { searchMusic } from '@/api'
await searchMusic('周杰伦', 'netease', 1, 20)

// ❌ 旧方式：需要判断环境
if (window.electronAPI) {
  await window.electronAPI.searchMusic(...)
} else {
  await searchMusicApi(...)
}
```

### 2. 易于维护

- 所有环境判断逻辑集中在适配层
- 修改调用策略只需修改适配层
- 业务代码无需修改

### 3. 支持 Web + Electron

- 同一套代码可以在 Web 和 Electron 中运行
- 自动选择最佳实现方式
- 无需为不同环境编写不同代码

### 4. 类型安全

- 完整的 TypeScript 类型定义
- IDE 自动补全和类型检查
- 减少运行时错误

## 📝 API 列表

### 音乐 API（统一使用前端 API）

- `searchMusic(keyword, source, page, count)` - 搜索音乐
- `getSongUrl(songId, source, quality)` - 获取歌曲 URL
- `getLyric(songId, source)` - 获取歌词
- `getPicUrl(picId, source, size)` - 获取封面 URL
- `getPlaylist(playlistId, limit, offset)` - 获取歌单

### 差异化实现 API（Web 和 Electron 都支持）

- `downloadMusic(params)` - 下载音乐（Web: <a> 标签，Electron: IPC + 文件对话框）
- `extractPalette(imageUrl)` - 提取封面颜色（Web: Canvas API，Electron: IPC 或 Canvas API）
- `showOpenDialog(options)` - 打开文件对话框（Web: <input type="file">，Electron: 原生对话框）
- `showSaveDialog(options)` - 保存文件对话框（Web: 返回默认文件名，Electron: 原生对话框）
- `importJson()` - 导入 JSON（Web: 文件输入，Electron: 原生对话框）
- `exportJson(params)` - 导出 JSON（Web: 文件下载，Electron: 原生对话框）

### 本地存储 API（差异化实现）

- `saveData(key, data)` - 保存数据（Web: localStorage，Electron: 文件系统）
- `loadData(key)` - 加载数据（Web: localStorage，Electron: 文件系统）
- `removeData(key)` - 删除数据（Web: localStorage，Electron: 文件系统）

### 环境检测

- `isElectron()` - 检测是否在 Electron 环境
- `isWeb()` - 检测是否在 Web 环境

## 🚨 注意事项

1. **不要直接导入 core.ts**：业务代码应该从 `@/api` 导入，使用适配层
2. **所有功能都支持 Web 和 Electron**：不再有 Electron 专用功能，所有功能都有 Web 实现
3. **差异化实现**：某些功能在 Web 和 Electron 中的实现方式不同，但接口统一
4. **Electron 主进程清理**：
   - 普通 API（搜索、获取 URL、歌词、封面等）已从 Electron 主进程移除
   - Electron 主进程只保留差异化处理需要的功能（下载、文件对话框、本地存储等）
   - 这减少了代码量，提高了性能，简化了维护
5. **文件组织**：
   - Web 工具函数位于 `src/utils/web-utils.ts`
   - 环境检测配置位于 `src/api/config.ts`
   - 适配层位于 `src/api/adapter.ts`
